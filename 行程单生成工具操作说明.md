# 行程单生成工具操作说明

## 一、用途

这个工具用于把订单 Excel 自动填入行程单 Word 模板，生成可编辑的 `.docx` 行程单。

生成内容包括：

- 旅游路线
- 开始时间、结束时间
- 领队、领队电话
- 客人数
- 旅客姓名、证件类型、证件号码、手机号

## 二、文件夹说明

项目文件夹位置：

```text
/Users/sean/Documents/自动化创建行程单
```

常用文件夹：

| 文件夹 | 用途 |
|---|---|
| `input/` | 放订单 Excel |
| `templates/` | 放行程单 Word 模板 |
| `output/` | 生成后的行程单 |
| `config/` | 默认配置 |
| `examples/` | 示例 PDF、旧模板等参考文件 |

## 三、使用前准备

1. 打开项目文件夹：

```text
/Users/sean/Documents/自动化创建行程单
```

2. 把订单 Excel 放进 `input/` 文件夹。

订单表文件名建议保持类似：

```text
订单列表_2026-05-21_812397290034958336.xlsx
```

3. 确认模板文件存在：

```text
templates/行程单通用模版.docx
```

## 四、生成行程单

打开终端，运行：

```bash
cd /Users/sean/Documents/自动化创建行程单
./run.sh
```

运行后终端会依次提示填写：

```text
旅游路线（回车使用默认值），输入完成后按回车
>
已记录旅游路线。

领队（多领队用、分隔，回车使用默认值），输入完成后按回车
>
领队电话（填写手机号，回车使用默认值），输入完成后按回车
>
```

填写说明：

- 旅游路线：填写完整行程路线。
- 领队：多个领队用 `、` 分隔，例如 `张三、李四`。
- 领队电话：填写需要展示在行程单上的领队电话。
- 直接按回车：使用默认值。
- 粘贴内容如果自带换行，会自动提交到下一项；如果发现填错，在确认页输入 `N` 重新填写。

## 五、确认信息

填写完成后，终端会显示预览信息，例如：

```text
请确认信息：
旅游路线：银川-五湖穿越-银川
领队：张三、李四
领队电话：13800000000
出行时间：2026 年 05 月 23 日 至 2026 年 05 月 30 日
表格路线：青甘大环线
生成文件：060523青甘大环线行程单.docx
确认生成？请输入 Y/N：
```

操作规则：

- 输入 `Y`：确认生成。
- 输入 `N`：取消本次填写，重新输入。
- 输入其他内容：直接退出，不生成行程单。

## 六、查看生成结果

生成后的文件在：

```text
output/
```

文件命名规则：

```text
日期编号 + 路线名 + 行程单.docx
```

示例：

```text
060523青甘大环线行程单.docx
```

## 七、订单表要求

订单表需要包含以下字段：

| 字段 | 用途 |
|---|---|
| `路线` | 用于生成文件名 |
| `出行时间` | 用于自动填写开始时间、结束时间 |
| `旅客姓名` | 填入旅客表格 |
| `旅客证件类型` | 填入旅客表格 |
| `旅客证件号码` | 填入旅客表格 |
| `旅客手机号码` | 填入旅客表格 |

如果旅客人数超过模板里的表格行数，工具会自动添加表格行。

## 八、常见问题

### 1. 提示找不到订单表格

确认订单 Excel 是否已经放到：

```text
input/
```

并且文件名是 `.xlsx` 格式。

### 2. 提示 Excel 缺少必要列

说明订单表字段不完整，需要确认表格里是否有：

```text
旅客姓名、旅客证件类型、旅客证件号码、旅客手机号码
```

### 3. 不想每次填写信息

可以运行：

```bash
./run.sh --no-prompt
```

这样会直接使用 `config/trip_config.json` 里的默认配置。

### 4. 生成后发现信息填错

重新运行：

```bash
./run.sh
```

在确认信息时输入 `N` 可以返回重新填写；确认无误后输入 `Y`。
